services:
  kyuboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kyuboard
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
